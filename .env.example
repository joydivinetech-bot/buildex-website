# Local preview defaults. Use the exact public HTTPS origin in production.
APP_ORIGIN=http://127.0.0.1:4174
HOST=127.0.0.1
PORT=4174
NODE_ENV=development
# Relative to this project. Store on a persistent disk in production.
DATA_DIR=./data
