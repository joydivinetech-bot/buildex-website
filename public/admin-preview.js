'use strict';
const text=(tag,value,className='')=>{const node=document.createElement(tag);node.textContent=value;if(className)node.className=className;return node;};
function applyProject(project){const grid=document.querySelector('#portfolio-grid');if(!grid||!project)return;const note=document.querySelector('.portfolio-note');if(note)note.textContent='Private project preview — this project is not published.';const photos=Array.isArray(project.photos)?project.photos:[];grid.replaceChildren();if(!photos.length){grid.append(text('p','Add at least one photo to preview the project.','gallery-empty'));return;}photos.forEach((photo,index)=>{const button=text('button','','media-card');button.type='button';const cover=text('span','','media-cover'),image=document.createElement('img');image.src='/admin/api/media?key='+encodeURIComponent(photo.key);image.alt=project.name+' — '+(photo.label||'Project photo');image.loading=index?'lazy':'eager';cover.append(image,text('span','PROJECT PREVIEW','media-tag'));const meta=text('span','','media-meta'),copy=text('span','','media-copy');copy.append(text('span',project.name,'media-title'),text('span',project.category+(project.location?' / '+project.location:''),'media-subtitle'));meta.append(copy,text('span','↗','media-arrow'));button.append(cover,meta);grid.append(button);});const count=document.querySelector('#media-count');if(count)count.textContent=photos.length+(photos.length===1?' item':' items');}
addEventListener('message',event=>{if(event.origin!==location.origin||event.source!==parent||event.data?.type!=='buildex-preview')return;applyProject(event.data.project);});
document.addEventListener('click',event=>{if(event.target.closest('a'))event.preventDefault();});
document.addEventListener('submit',event=>{event.preventDefault();event.stopImmediatePropagation();},true);
parent.postMessage({type:'buildex-preview-ready'},location.origin);
