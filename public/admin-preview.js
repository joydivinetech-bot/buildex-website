'use strict';
const text=(tag,value,className='')=>{const node=document.createElement(tag);node.textContent=value;if(className)node.className=className;return node;};
function apply(settings){
 const primary=settings.primaryPhone,secondary=settings.secondaryPhone,hero=document.querySelector('.hero-content');
 if(hero){const eyebrow=hero.querySelector(':scope > .eyebrow'),lines=hero.querySelectorAll('.hero-title-line'),description=hero.querySelector(':scope > p');if(eyebrow&&settings.heroEyebrow)eyebrow.lastChild.textContent=' '+settings.heroEyebrow;if(lines[0])lines[0].textContent=settings.heroTitleLine1||'';if(lines[1])lines[1].textContent=settings.heroTitleLine2||'';if(description){description.replaceChildren(...String(settings.heroDescription||'').split('\n').flatMap((line,index)=>index?[document.createElement('br'),document.createTextNode(line)]:[document.createTextNode(line)]));}}
 const topbar=document.querySelector('.topbar span');if(topbar&&settings.serviceArea)topbar.lastChild.textContent=' Proudly serving '+settings.serviceArea+' & surrounding areas';
 document.querySelectorAll('a[href^="tel:"]').forEach(link=>{if(!primary)return;link.href='tel:'+primary.replace(/[^+\d]/g,'');if(/^\(832\)/.test(link.textContent.trim()))link.textContent=primary;});
 const contact=document.querySelector('.contact-details');if(contact){const rows=[...contact.children],phoneRow=rows[0]?.querySelector('span'),emailRow=rows.find(row=>row.querySelector('.lucide-mail'))?.querySelector('span'),hoursRow=rows.find(row=>row.querySelector('.lucide-clock'))?.querySelector('span'),addressRow=rows.find(row=>row.querySelector('.lucide-map-pin'))?.querySelector('span');if(phoneRow&&primary){const label=phoneRow.querySelector('small'),link=text('a',primary);link.href='tel:'+primary.replace(/[^+\d]/g,'');phoneRow.replaceChildren(label,link);if(secondary){const extra=text('span','','additional-phones'),second=text('a',secondary);second.href='tel:'+secondary.replace(/[^+\d]/g,'');extra.append(second);phoneRow.append(extra);}}if(emailRow&&settings.email){const label=emailRow.querySelector('small'),link=text('a',settings.email);link.href='mailto:'+settings.email;emailRow.replaceChildren(label,link);}if(hoursRow&&settings.hours){const label=hoursRow.querySelector('small');hoursRow.replaceChildren(label,document.createTextNode(settings.hours));}if(addressRow&&settings.serviceArea){const label=addressRow.querySelector('small');addressRow.replaceChildren(label,document.createTextNode(settings.serviceArea));}}
 const footer=[...document.querySelectorAll('.footer p')].find(node=>node.textContent.includes('Residential'));if(footer&&settings.serviceArea)footer.innerHTML='Residential &amp; commercial construction<br>'+settings.serviceArea.replace(/[&<>]/g,value=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[value]));
}
function applyProject(project){const grid=document.querySelector('#portfolio-grid');if(!grid||!project)return;const note=document.querySelector('.portfolio-note');if(note)note.textContent='Private project preview — this project is not published.';const photos=Array.isArray(project.photos)?project.photos:[];grid.replaceChildren();if(!photos.length){grid.append(text('p','Add at least one photo to preview the project.','gallery-empty'));return;}photos.forEach((photo,index)=>{const button=text('button','','media-card');button.type='button';const cover=text('span','','media-cover'),image=document.createElement('img');image.src='/admin/api/media?key='+encodeURIComponent(photo.key);image.alt=project.name+' — '+(photo.label||'Project photo');image.loading=index?'lazy':'eager';cover.append(image,text('span','PROJECT PREVIEW','media-tag'));const meta=text('span','','media-meta'),copy=text('span','','media-copy');copy.append(text('span',project.name,'media-title'),text('span',project.category+(project.location?' / '+project.location:''),'media-subtitle'));meta.append(copy,text('span','↗','media-arrow'));button.append(cover,meta);grid.append(button);});const count=document.querySelector('#media-count');if(count)count.textContent=photos.length+(photos.length===1?' item':' items');}
addEventListener('message',event=>{if(event.origin!==location.origin||event.source!==parent||event.data?.type!=='buildex-preview')return;apply(event.data.settings||{});applyProject(event.data.project);});
document.addEventListener('click',event=>{if(event.target.closest('a'))event.preventDefault();});
document.addEventListener('submit',event=>{event.preventDefault();event.stopImmediatePropagation();},true);
parent.postMessage({type:'buildex-preview-ready'},location.origin);
